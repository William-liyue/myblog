<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myblog.mapper.ResourceMapper">

    <!--新增保存资源-->
    <insert id="saveResource" parameterType="com.myblog.mapper.ResourceMapper">
        insert into myblog.t_resource (id, resourceName, resourceAddress, firstResource, secondResource)
            values (#{id},#{resourceName},#{resourceAddress},#{firstResource},#{secondResource})
    </insert>

    <!--查询资源-->
    <select id="listResource" resultType="com.myblog.entity.Resource">
        select * from myblog.t_resource order by t_resource.id desc
    </select>

    <!--根据id查询资源-->
    <select id="getResource" resultType="com.myblog.entity.Resource">
        select * from myblog.t_resource where id = #{id}
    </select>

    <!--编辑资源-->
    <update id="updateResource">
        update myblog.t_resource set
        id = #{id},resourceName = #{resourceName},resourceAddress = #{resourceAddress},firstResource = #{firstResource},secondResource = #{secondResource}
        where id = #{id}
    </update>

    <!--删除资源-->
    <delete id="deleteResource">
        delete from myblog.t_resource where id = #{id}
    </delete>
</mapper>